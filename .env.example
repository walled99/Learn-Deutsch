# LernDeutsch AI - Environment Variables Template
# Copy this to .env and fill in your actual values

# Supabase Configuration (Client-side safe)
# These are used by the mobile app to communicate with Supabase.
# They are public keys and are safe to be bundled in the app.
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Gemini AI Configuration (Client-side safe)
# The API key for Gemini 1.5 Flash vocabulary extraction.
EXPO_PUBLIC_GEMINI_API_KEY=your-gemini-api-key

# Application Environment
# Used for build-time configurations (development, production, etc.)
APP_ENV=development

# -----------------------------------------------------------------------------
# WARNING: DO NOT ADD SERVER-SIDE SECRETS BELOW
# -----------------------------------------------------------------------------
# The following keys were REMOVED from the client-side .env for security:
# - SUPABASE_SERVICE_ROLE_KEY
# - SUPABASE_DB_URL
# - SUPABASE_JWT_SECRET
# - INNGEST_EVENT_KEY
# - INNGEST_SIGNING_KEY
# - FASTAPI_SECRET_KEY
